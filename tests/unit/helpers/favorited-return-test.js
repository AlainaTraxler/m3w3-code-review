
import { favoritedReturn } from 'tifun-a/helpers/favorited-return';
import { module, test } from 'qunit';

module('Unit | Helper | favorited return');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = favoritedReturn([42]);
  assert.ok(result);
});

