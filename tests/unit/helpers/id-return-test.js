
import { idReturn } from 'tifun-a/helpers/id-return';
import { module, test } from 'qunit';

module('Unit | Helper | id return');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = idReturn([42]);
  assert.ok(result);
});

